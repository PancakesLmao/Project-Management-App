<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProjectManagement" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/tea</item>
        <item name="colorPrimaryVariant">@color/darkTea</item>
        <item name="colorOnPrimary">@color/lightTea</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/tea</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/darkTea</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Background color. -->
        <item name="android:windowBackground">@color/lightTea</item>
        <!-- Default text color. -->
        <item name="android:textColor">@color/darkTea</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.ProjectManagement.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ProjectManagement.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ProjectManagement.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>